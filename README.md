# Arduino-RFID_LOCK-OLED_DISPLAY-HW-136-KEYPAD.
Arduino uno + RC22 rfid reader smart lock displaying state if lock on OLED display. also having a capability to access manually trough capacitive keypad.
